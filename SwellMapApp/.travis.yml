language: java
jdk:
 - openjdk8
 - openjdk11
install: true
script: ./mvnw clean install
cache:
  directories:
  - $HOME/.m2

script:
  - ./mvnw clean install
  - bash <(curl -s https://codecov.io/bash)